# 🏗️ Urikkiri 프로젝트 아키텍처 구조도

## 📋 목차
1. [전체 시스템 아키텍처](#전체-시스템-아키텍처)
2. [레이어 아키텍처](#레이어-아키텍처)
3. [도메인 구조](#도메인-구조)
4. [WebSocket 통신 플로우](#websocket-통신-플로우)
5. [보안 아키텍처](#보안-아키텍처)
6. [데이터베이스 구조](#데이터베이스-구조)

---

## 🌐 전체 시스템 아키텍처

```
┌─────────────────────────────────────────────────────────────────┐
│                         Unity Client                              │
│                      (Frontend Application)                       │
└──────────────────┬────────────────────────────┬───────────────────┘
                   │                            │
                   │ REST API                   │ WebSocket
                   │ (HTTP/HTTPS)               │ (ws:/wss:)
                   │                            │
┌──────────────────▼────────────────────────────▼───────────────────┐
│                                                                    │
│                    Spring Boot Application                         │
│                    (Urikkiri-Server)                              │
│                                                                    │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │              Security Layer                               │   │
│  │  - JWT Authentication                                     │   │
│  │  - ExceptionFilter                                        │   │
│  │  - SecurityConfig                                         │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                    │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │              Presentation Layer                           │   │
│  │  - REST Controllers (User, Card, Quiz)                    │   │
│  │  - WebSocket Handler                                      │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                    │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │              Service Layer                                │   │
│  │  - Business Logic                                         │   │
│  │  - Game Round Manager                                     │   │
│  │  - WebSocket Session Manager                              │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                    │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │              Domain Layer                                 │   │
│  │  - Entities (User, Card, Quiz, Room, Participant)        │   │
│  │  - Repositories                                           │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                    │
└────────────────────────────┬───────────────────────────────────────┘
                             │
                             │ JPA/Hibernate
                             │
┌────────────────────────────▼───────────────────────────────────────┐
│                       MySQL Database                               │
│  - users                                                           │
│  - cards                                                           │
│  - quizzes                                                         │
│  - rooms                                                           │
│  - participants                                                    │
└────────────────────────────────────────────────────────────────────┘
```

---

## 🏛️ 레이어 아키텍처

```
┌─────────────────────────────────────────────────────────────────────┐
│                         클라이언트 레이어                              │
│                     (Unity Frontend Client)                          │
└─────────────────────────────────────────────────────────────────────┘
                                  ▲
                                  │
                    REST API / WebSocket Protocol
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────┐
│                        프레젠테이션 레이어                             │
├─────────────────────────────────────────────────────────────────────┤
│  REST Controllers:                                                  │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐                │
│  │    User     │  │    Card     │  │    Quiz     │                │
│  │ Controller  │  │ Controller  │  │ Controller  │                │
│  └─────────────┘  └─────────────┘  └─────────────┘                │
│                                                                      │
│  WebSocket Handler:                                                 │
│  ┌──────────────────────────────────────────────────┐              │
│  │          WebSocketHandler                        │              │
│  │  - handleTextMessage()                           │              │
│  │  - afterConnectionEstablished()                  │              │
│  │  - afterConnectionClosed()                       │              │
│  └──────────────────────────────────────────────────┘              │
└─────────────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────┐
│                          서비스 레이어                                 │
├─────────────────────────────────────────────────────────────────────┤
│  Domain Services:                                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐             │
│  │ User Service │  │ Card Service │  │ Quiz Service │             │
│  │ - SignUp     │  │ - GetRandom  │  │ - GetRandom  │             │
│  │ - Login      │  │   Cards      │  │   Quiz       │             │
│  │ - Logout     │  └──────────────┘  └──────────────┘             │
│  │ - Ranking    │                                                   │
│  └──────────────┘                                                   │
│                                                                      │
│  ┌──────────────┐  ┌──────────────┐                                │
│  │ Play Service │  │    Global    │                                │
│  │ - CreateRoom │  │   Services   │                                │
│  │ - JoinRoom   │  │ - GameRound  │                                │
│  └──────────────┘  │   Manager    │                                │
│                    │ - WebSocket  │                                │
│                    │   Session    │                                │
│                    │   Manager    │                                │
│                    └──────────────┘                                │
└─────────────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────┐
│                          도메인 레이어                                 │
├─────────────────────────────────────────────────────────────────────┤
│  Entities:                                                          │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐           │
│  │   User   │  │   Card   │  │   Quiz   │  │   Room   │           │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘           │
│  ┌──────────────┐                                                   │
│  │ Participant  │                                                   │
│  └──────────────┘                                                   │
│                                                                      │
│  Repositories:                                                      │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐       │
│  │ UserRepository │  │ CardRepository │  │ QuizRepository │       │
│  └────────────────┘  └────────────────┘  └────────────────┘       │
│  ┌────────────────┐  ┌─────────────────────┐                       │
│  │ RoomRepository │  │ ParticipantRepository│                      │
│  └────────────────┘  └─────────────────────┘                       │
└─────────────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────┐
│                       데이터베이스 레이어                              │
│                         (MySQL Database)                            │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 📦 도메인 구조

```
src/main/java/com/example/urikkiriserver/
│
├── UrikkiriServerApplication.java          # 메인 애플리케이션
│
├── domain/                                  # 도메인 계층
│   │
│   ├── user/                                # 사용자 도메인
│   │   ├── domain/
│   │   │   ├── User.java                   # 사용자 엔티티
│   │   │   └── repository/
│   │   │       └── UserRepository.java     # 사용자 Repository
│   │   ├── service/
│   │   │   ├── SignUpService.java          # 회원가입
│   │   │   ├── LoginService.java           # 로그인
│   │   │   ├── LogoutService.java          # 로그아웃
│   │   │   ├── TotalRankingService.java    # 랭킹 조회
│   │   │   ├── QueryMyPageService.java     # 마이페이지
│   │   │   └── UserFacade.java             # Facade 패턴
│   │   ├── presentation/
│   │   │   ├── UserController.java         # REST Controller
│   │   │   └── dto/
│   │   │       ├── request/
│   │   │       │   ├── SignUpRequest.java
│   │   │       │   └── LoginRequest.java
│   │   │       └── response/
│   │   │           ├── TokenResponse.java
│   │   │           ├── MyPageResponse.java
│   │   │           └── UserRankingResponse.java
│   │   └── exception/
│   │       ├── UserNotFound.java
│   │       ├── UserExists.java
│   │       └── PasswordMisMatch.java
│   │
│   ├── card/                                # 카드 도메인
│   │   ├── domain/
│   │   │   ├── Card.java                   # 카드 엔티티
│   │   │   └── repository/
│   │   │       └── CardRepository.java
│   │   ├── service/
│   │   │   └── QueryRandomCardsService.java # 랜덤 카드 조회
│   │   ├── presentation/
│   │   │   └── dto/
│   │   │       └── response/
│   │   │           ├── CardResponse.java
│   │   │           └── CardListResponse.java
│   │   └── exception/
│   │       ├── CardNotFoundException.java
│   │       └── InsufficientCardsException.java
│   │
│   ├── quiz/                                # 퀴즈 도메인
│   │   ├── domain/
│   │   │   ├── Quiz.java                   # 퀴즈 엔티티
│   │   │   └── repository/
│   │   │       └── QuizRepository.java
│   │   ├── service/
│   │   │   └── QueryRandomQuizService.java # 랜덤 퀴즈 조회
│   │   ├── presentation/
│   │   │   └── dto/
│   │   │       └── response/
│   │   │           └── QuizResponse.java
│   │   └── exception/
│   │       └── QuizNotFoundException.java
│   │
│   └── play/                                # 게임 플레이 도메인
│       ├── domain/
│       │   ├── Room.java                   # 방 엔티티
│       │   ├── Participant.java            # 참가자 엔티티
│       │   └── repository/
│       │       ├── RoomRepository.java
│       │       └── ParticipantRepository.java
│       ├── service/
│       │   ├── CreateRoomService.java      # 방 생성
│       │   └── JoinRoomService.java        # 방 참가
│       ├── presentation/
│       │   ├── PlayController.java         # REST Controller
│       │   └── dto/
│       │       └── response/
│       │           ├── RoomResponse.java
│       │           └── JoinRoomResponse.java
│       └── exception/
│           ├── RoomNotFoundException.java
│           ├── RoomAlreadyFullException.java
│           ├── AlreadyInRoomException.java
│           ├── ParticipantNotFoundException.java
│           ├── ExaminerNotFoundException.java
│           └── ExaminerCannotSubmitCardException.java
│
└── global/                                  # 전역 설정 및 공통 기능
    │
    ├── config/
    │   └── WebSocketConfig.java            # WebSocket 설정
    │
    ├── security/                           # 보안 관련
    │   ├── SecurityConfig.java             # Spring Security 설정
    │   ├── auth/
    │   │   ├── AuthDetails.java
    │   │   └── AuthDetailsService.java
    │   └── jwt/
    │       ├── JwtProvider.java            # JWT 생성/검증
    │       ├── JwtFilter.java              # JWT 필터
    │       └── JwtProperties.java          # JWT 설정
    │
    ├── websocket/                          # WebSocket 관련
    │   ├── WebSocketHandler.java           # WebSocket 핸들러
    │   ├── WebSocketSessionManager.java    # 세션 관리
    │   ├── GameRoundManager.java           # 게임 라운드 관리
    │   ├── CustomHandshakeInterceptor.java # 핸드셰이크 인터셉터
    │   ├── ClientType.java                 # 클라이언트 타입
    │   ├── WebSocketUtils.java             # 유틸리티
    │   ├── dto/
    │   │   ├── WebSocketMessage.java       # 메시지 기본 구조
    │   │   ├── WebSocketMessageType.java   # 메시지 타입 enum
    │   │   ├── GameReadyData.java
    │   │   ├── GameStartData.java
    │   │   ├── ExaminerSelectionDto.java
    │   │   ├── SubmittedCardInfo.java
    │   │   ├── NextRoundData.java
    │   │   ├── GameResultDto.java
    │   │   ├── PlayerRankInfo.java
    │   │   ├── ParticipantInfo.java
    │   │   └── UserExitDto.java
    │   └── exception/
    │       ├── WebSocketAuthenticationRequired.java
    │       ├── WebSocketRoomCreationFailed.java
    │       ├── WebSocketRoomNotFound.java
    │       ├── WebSocketNotParticipant.java
    │       ├── WebSocketInvalidMessageFormat.java
    │       └── WebSocketRoomCodeRequired.java
    │
    ├── error/                              # 에러 핸들링
    │   ├── GlobalExceptionHandler.java     # 전역 예외 처리
    │   ├── ExceptionFilter.java            # 예외 필터
    │   └── exception/
    │       ├── ErrorCode.java              # 에러 코드 enum
    │       ├── ErrorResponse.java          # 에러 응답 DTO
    │       └── UrikkiriException.java      # 커스텀 예외
    │
    ├── exception/                          # 전역 예외
    │   ├── InvalidJwt.java
    │   └── ExpiredJwt.java
    │
    └── utils/                              # 유틸리티
        └── WebSocketUtils.java
```

---

## 🔄 WebSocket 통신 플로우

### 전체 게임 플로우

```
┌─────────────┐                                  ┌─────────────┐
│   Client    │                                  │   Server    │
└──────┬──────┘                                  └──────┬──────┘
       │                                                │
       │  1. WebSocket 연결 (ws://server/game)         │
       ├──────────────────────────────────────────────>│
       │                                                │
       │  2. Connection Established                    │
       │<──────────────────────────────────────────────┤
       │                                                │
       │  3. CREATE_ROOM or JOIN_ROOM                  │
       ├──────────────────────────────────────────────>│
       │     { type: "CREATE_ROOM" }                   │
       │                                                │
       │  4. ROOM_CREATED or USER_JOINED               │
       │<──────────────────────────────────────────────┤
       │     { room_code, participants }                │
       │     (USER_JOINED sent to all participants)    │
       │                                                │
       │  5. GAME_READY (when 4 players joined)        │
       │<══════════════════════════════════════════════┤
       │     { participants[], currentRound }          │
       │                                                │
       │  7. SELECT_EXAMINER                           │
       ├──────────────────────────────────────────────>│
       │     { examinerId }                            │
       │                                                │
       │  8. GAME_START (broadcast)                    │
       │<══════════════════════════════════════════════┤
       │     { examiner, quiz, roundNumber }           │
       │                                                │
       │  9. SUBMIT_CARD (players only)                │
       ├──────────────────────────────────────────────>│
       │     { cardId, text }                          │
       │                                                │
       │  10. CARD_SUBMITTED (to all)                  │
       │<══════════════════════════════════════════════┤
       │      { submittedCount }                       │
       │                                                │
       │  11. ALL_CARDS_SUBMITTED (when all done)      │
       │<══════════════════════════════════════════════┤
       │      { submittedCards[] }                     │
       │                                                │
       │  12. SELECT_WINNER (examiner only)            │
       ├──────────────────────────────────────────────>│
       │      { winnerId }                             │
       │                                                │
       │  13. ROUND_END (broadcast)                    │
       │<══════════════════════════════════════════════┤
       │      { winner, currentScores[] }              │
       │                                                │
       │  14. NEXT_ROUND or GAME_END                   │
       ├──────────────────────────────────────────────>│
       │                                                │
       │  15a. NEXT_ROUND_START (if continue)          │
       │<══════════════════════════════════════════════┤
       │       { newQuiz, examiner, roundNumber }      │
       │                                                │
       │  15b. GAME_END (if rounds complete)           │
       │<══════════════════════════════════════════════┤
       │       { rankings[], finalScores[] }           │
       │                                                │
       │  16. Disconnect                               │
       ├──────────────────────────────────────────────>│
       │                                                │
       │  17. USER_EXIT (broadcast to others)          │
       │   ═════════════════════════════════════════>  │
       │                                                │
```

### WebSocket 메시지 타입별 처리

```
┌────────────────────────────────────────────────────────────────────┐
│                     WebSocketHandler                               │
├────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  handleTextMessage(session, message)                               │
│       │                                                             │
│       ├─ Parse JSON → WebSocketMessage                             │
│       │                                                             │
│       └─ Switch by MessageType:                                    │
│                                                                     │
│          ┌─────────────────────────────────────────┐               │
│          │ CREATE_ROOM                             │               │
│          │  → CreateRoomService                    │               │
│          │  → Store session                        │               │
│          │  → Send ROOM_CREATED                    │               │
│          └─────────────────────────────────────────┘               │
│                                                                     │
│          ┌─────────────────────────────────────────┐               │
│          │ JOIN_ROOM                               │               │
│          │  → JoinRoomService                      │               │
│          │  → Validate room & capacity             │               │
│          │  → Broadcast USER_JOINED                │               │
│          │  → Check if ready (4 players)           │               │
│          │  → Send GAME_READY if ready             │               │
│          └─────────────────────────────────────────┘               │
│                                                                     │
│          ┌─────────────────────────────────────────┐               │
│          │ SELECT_EXAMINER                         │               │
│          │  → Validate host permission             │               │
│          │  → Set examiner in GameRoundManager     │               │
│          │  → Get random quiz                      │               │
│          │  → Broadcast GAME_START                 │               │
│          └─────────────────────────────────────────┘               │
│                                                                     │
│          ┌─────────────────────────────────────────┐               │
│          │ SUBMIT_CARD                             │               │
│          │  → Validate not examiner                │               │
│          │  → Store card submission                │               │
│          │  → Broadcast CARD_SUBMITTED             │               │
│          │  → Check if all submitted               │               │
│          │  → Broadcast ALL_CARDS_SUBMITTED        │               │
│          └─────────────────────────────────────────┘               │
│                                                                     │
│          ┌─────────────────────────────────────────┐               │
│          │ SELECT_WINNER                           │               │
│          │  → Validate examiner permission         │               │
│          │  → Update scores                        │               │
│          │  → Broadcast ROUND_END                  │               │
│          └─────────────────────────────────────────┘               │
│                                                                     │
│          ┌─────────────────────────────────────────┐               │
│          │ NEXT_ROUND                              │               │
│          │  → Check round limit                    │               │
│          │  → If continue:                         │               │
│          │     → Get new quiz                      │               │
│          │     → Broadcast NEXT_ROUND_START        │               │
│          │  → If end:                              │               │
│          │     → Calculate rankings                │               │
│          │     → Update user stats                 │               │
│          │     → Broadcast GAME_END                │               │
│          └─────────────────────────────────────────┘               │
│                                                                     │
└────────────────────────────────────────────────────────────────────┘
```

### 세션 관리 구조

```
┌─────────────────────────────────────────────────────────────────┐
│              WebSocketSessionManager                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Data Structures:                                               │
│  ┌──────────────────────────────────────────────┐              │
│  │ Map<String, WebSocketSession>                │              │
│  │ sessionId → WebSocketSession                 │              │
│  └──────────────────────────────────────────────┘              │
│                                                                  │
│  ┌──────────────────────────────────────────────┐              │
│  │ Map<String, String>                          │              │
│  │ sessionId → roomCode                         │              │
│  └──────────────────────────────────────────────┘              │
│                                                                  │
│  ┌──────────────────────────────────────────────┐              │
│  │ Map<String, Long>                            │              │
│  │ sessionId → userId                           │              │
│  └──────────────────────────────────────────────┘              │
│                                                                  │
│  Methods:                                                       │
│  - addSession(session, user)                                   │
│  - removeSession(sessionId)                                    │
│  - associateSessionWithRoom(sessionId, roomCode)               │
│  - getSessionsByRoom(roomCode)                                 │
│  - broadcastToRoom(room_code, message)                          │
│  - sendToSession(sessionId, message)                           │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│              GameRoundManager                                   │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Data Structures (per room):                                   │
│  ┌──────────────────────────────────────────────┐              │
│  │ Map<String, GameRoundState>                  │              │
│  │ roomCode → GameRoundState {                  │              │
│  │   - currentRound: int                        │              │
│  │   - examinerId: Long                         │              │
│  │   - currentQuiz: Quiz                        │              │
│  │   - submittedCards: Map<Long, CardData>      │              │
│  │   - scores: Map<Long, Integer>               │              │
│  │   - roundStartTime: Timestamp                │              │
│  │ }                                            │              │
│  └──────────────────────────────────────────────┘              │
│                                                                  │
│  Methods:                                                       │
│  - initializeRound(room_code, examinerId, quiz)                 │
│  - submitCard(room_code, userId, card)                          │
│  - selectWinner(room_code, winnerId)                            │
│  - nextRound(roomCode)                                         │
│  - endGame(roomCode)                                           │
│  - getGameState(roomCode)                                      │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔐 보안 아키텍처

```
┌─────────────────────────────────────────────────────────────────┐
│                      Client Request                             │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                   ExceptionFilter                               │
│  - Catches all exceptions                                       │
│  - Handles JWT exceptions                                       │
│  - Returns proper error response                                │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                    JwtFilter                                    │
│  1. Extract Authorization header                                │
│  2. Validate Bearer token                                       │
│  3. JwtProvider.validateToken(token)                            │
│  4. Extract user info from token                                │
│  5. Set Authentication in SecurityContext                       │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│               Spring Security Filter Chain                      │
│  - SecurityConfig                                               │
│  - Permit: /api/users/signup, /api/users/login                 │
│  - Authenticate: All other endpoints                            │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Controllers                                  │
│  - @AuthenticationPrincipal User user                           │
│  - Access authenticated user info                               │
└─────────────────────────────────────────────────────────────────┘

JWT Token Structure:
┌──────────────────────────────────────────────────────────────┐
│ Header                                                        │
│ {                                                             │
│   "alg": "HS256",                                            │
│   "typ": "JWT"                                               │
│ }                                                             │
├──────────────────────────────────────────────────────────────┤
│ Payload                                                       │
│ {                                                             │
│   "sub": "user_id",                                           │
│   "email": "user@example.com",                               │
│   "iat": 1234567890,                                         │
│   "exp": 1234571490                                          │
│ }                                                             │
├──────────────────────────────────────────────────────────────┤
│ Signature                                                     │
│ HMACSHA256(                                                   │
│   base64UrlEncode(header) + "." +                            │
│   base64UrlEncode(payload),                                  │
│   secret                                                      │
│ )                                                             │
└──────────────────────────────────────────────────────────────┘

WebSocket Authentication:
┌──────────────────────────────────────────────────────────────┐
│ CustomHandshakeInterceptor                                    │
│  1. beforeHandshake()                                        │
│  2. Extract token from query param or header                 │
│  3. Validate JWT token                                       │
│  4. Store user info in session attributes                    │
│  5. Allow/Reject connection                                  │
└──────────────────────────────────────────────────────────────┘
```

---

## 💾 데이터베이스 구조

```
┌─────────────────────────────────────────────────────────────────┐
│                            users                                │
├─────────────────────────────────────────────────────────────────┤
│  id              BIGINT         PK, AUTO_INCREMENT              │
│  email           VARCHAR(255)   UNIQUE, NOT NULL                │
│  password        VARCHAR(255)   NOT NULL (BCrypt)               │
│  nickname        VARCHAR(50)    NOT NULL                        │
│  total_score     INT            DEFAULT 0                       │
│  total_wins      INT            DEFAULT 0                       │
│  total_games     INT            DEFAULT 0                       │
│  created_at      TIMESTAMP      DEFAULT CURRENT_TIMESTAMP       │
│  updated_at      TIMESTAMP      DEFAULT CURRENT_TIMESTAMP       │
└─────────────────────────────────────────────────────────────────┘
                          │
                          │ 1:N
                          │
┌─────────────────────────▼───────────────────────────────────────┐
│                       participants                              │
├─────────────────────────────────────────────────────────────────┤
│  id              BIGINT         PK, AUTO_INCREMENT              │
│  user_id         BIGINT         FK → users.id                   │
│  room_id         BIGINT         FK → rooms.id                   │
│  score           INT            DEFAULT 0                       │
│  is_host         BOOLEAN        DEFAULT false                   │
│  is_examiner     BOOLEAN        DEFAULT false                   │
│  joined_at       TIMESTAMP      DEFAULT CURRENT_TIMESTAMP       │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          │ N:1
                          │
┌─────────────────────────▼───────────────────────────────────────┐
│                         rooms                                   │
├─────────────────────────────────────────────────────────────────┤
│  id              BIGINT         PK, AUTO_INCREMENT              │
│  room_code       VARCHAR(6)     UNIQUE, NOT NULL                │
│  host_id         BIGINT         FK → users.id                   │
│  current_round   INT            DEFAULT 0                       │
│  max_rounds      INT            DEFAULT 5                       │
│  status          VARCHAR(20)    (WAITING/READY/PLAYING/ENDED)  │
│  created_at      TIMESTAMP      DEFAULT CURRENT_TIMESTAMP       │
│  updated_at      TIMESTAMP      DEFAULT CURRENT_TIMESTAMP       │
└─────────────────────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────┐
│                           cards                                 │
├─────────────────────────────────────────────────────────────────┤
│  id              BIGINT         PK, AUTO_INCREMENT              │
│  text            VARCHAR(255)   NOT NULL                        │
│  category        VARCHAR(50)                                    │
│  created_at      TIMESTAMP      DEFAULT CURRENT_TIMESTAMP       │
└─────────────────────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────┐
│                          quizzes                                │
├─────────────────────────────────────────────────────────────────┤
│  id              BIGINT         PK, AUTO_INCREMENT              │
│  question        VARCHAR(500)   NOT NULL                        │
│  category        VARCHAR(50)                                    │
│  difficulty      VARCHAR(20)    (EASY/MEDIUM/HARD)             │
│  created_at      TIMESTAMP      DEFAULT CURRENT_TIMESTAMP       │
└─────────────────────────────────────────────────────────────────┘


Entity Relationships:
┌──────────┐                    ┌──────────────┐
│   User   │───────────────────>│ Participant  │
│          │        1:N         │              │
└──────────┘                    └──────┬───────┘
                                       │
                                       │ N:1
                                       │
┌──────────┐                    ┌──────▼───────┐
│   User   │───────────────────>│     Room     │
│ (Host)   │        1:N         │              │
└──────────┘                    └──────────────┘

Indexes:
- users.email (UNIQUE)
- rooms.room_code (UNIQUE)
- participants.user_id
- participants.room_id
- participants(room_id, user_id) (COMPOSITE)
```

---

## 🎮 게임 플레이 상태 머신

```
┌─────────────────────────────────────────────────────────────────┐
│                      Room Status Flow                           │
└─────────────────────────────────────────────────────────────────┘

        CREATE_ROOM
             │
             ▼
      ┌──────────────┐
      │   WAITING    │  (1-3 players)
      └──────┬───────┘
             │ JOIN_ROOM (4th player)
             ▼
      ┌──────────────┐
      │    READY     │  (4 players, waiting for examiner)
      └──────┬───────┘
             │ SELECT_EXAMINER
             ▼
      ┌──────────────┐
      │   PLAYING    │──┐
      └──────┬───────┘  │
             │          │
             │ Round    │ NEXT_ROUND
             │ Loop     │
             │          │
             └──────────┘
             │ All rounds complete
             ▼
      ┌──────────────┐
      │    ENDED     │
      └──────────────┘
             │
             │ Cleanup after 5 min
             ▼
         [Deleted]


┌─────────────────────────────────────────────────────────────────┐
│                    Round State Machine                          │
└─────────────────────────────────────────────────────────────────┘

                GAME_START
                     │
                     ▼
              ┌─────────────┐
              │ EXAMINER    │
              │  SELECTED   │
              └──────┬──────┘
                     │ Quiz assigned
                     ▼
              ┌─────────────┐
              │  CARD       │
              │ SUBMISSION  │◄───┐
              └──────┬──────┘    │ SUBMIT_CARD
                     │           │ (players)
                     │           │
                     │ All submitted
                     ▼
              ┌─────────────┐
              │   JUDGING   │
              └──────┬──────┘
                     │ SELECT_WINNER
                     ▼
              ┌─────────────┐
              │  ROUND_END  │
              └──────┬──────┘
                     │
          ┌──────────┴──────────┐
          │                     │
     More rounds?          Last round?
          │                     │
          ▼                     ▼
    NEXT_ROUND              GAME_END
```

---

## 📡 API 엔드포인트 맵

### REST API

```
┌─────────────────────────────────────────────────────────────────┐
│                       User API                                  │
├─────────────────────────────────────────────────────────────────┤
│  POST   /api/users/signup          회원가입                     │
│  POST   /api/users/login           로그인                       │
│  POST   /api/users/logout          로그아웃                     │
│  GET    /api/users/mypage          마이페이지 조회              │
│  GET    /api/users/ranking         전체 랭킹 조회               │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                       Card API                                  │
├─────────────────────────────────────────────────────────────────┤
│  GET    /api/cards/random          랜덤 카드 조회 (5장)         │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                       Quiz API                                  │
├─────────────────────────────────────────────────────────────────┤
│  GET    /api/quiz/random           랜덤 퀴즈 조회               │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                       Play API                                  │
├─────────────────────────────────────────────────────────────────┤
│  POST   /api/play/create           방 생성                      │
│  POST   /api/play/join             방 참가                      │
└─────────────────────────────────────────────────────────────────┘
```

### WebSocket API

```
┌─────────────────────────────────────────────────────────────────┐
│                    WebSocket Endpoint                           │
├─────────────────────────────────────────────────────────────────┤
│  ws://server/game                  게임 WebSocket 연결          │
│  (with JWT token in query or header)                           │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│              Client → Server Messages                           │
├─────────────────────────────────────────────────────────────────┤
│  CREATE_ROOM            방 생성                                 │
│  JOIN_ROOM              방 참가                                 │
│  SELECT_EXAMINER        출제자 선택                             │
│  SUBMIT_CARD            카드 제출                               │
│  SELECT_WINNER          우승자 선택                             │
│  NEXT_ROUND             다음 라운드                             │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│              Server → Client Messages                           │
├─────────────────────────────────────────────────────────────────┤
│  ROOM_CREATED           방 생성 완료                            │
│  USER_JOINED            방 참가 완료 (전체 참가자 목록)         │
│  USER_EXIT              사용자 퇴장 알림                        │
│  GAME_READY             게임 준비 완료 (4명)                   │
│  GAME_START             게임 시작                               │
│  CARD_SUBMITTED         카드 제출 알림                          │
│  ALL_CARDS_SUBMITTED    모든 카드 제출 완료                     │
│  ROUND_END              라운드 종료                             │
│  NEXT_ROUND_START       다음 라운드 시작                        │
│  GAME_END               게임 종료                               │
│  ERROR                  에러 메시지                             │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔧 기술 스택

```
┌─────────────────────────────────────────────────────────────────┐
│                        Backend                                  │
├─────────────────────────────────────────────────────────────────┤
│  Framework:        Spring Boot 3.x                              │
│  Language:         Java 17                                      │
│  Build Tool:       Gradle                                       │
│  Security:         Spring Security + JWT                        │
│  WebSocket:        Spring WebSocket (STOMP over WebSocket)     │
│  ORM:              Spring Data JPA (Hibernate)                  │
│  Database:         MySQL 8.x                                    │
│  Validation:       Jakarta Validation                           │
│  JSON:             Jackson                                      │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                        Frontend                                 │
├─────────────────────────────────────────────────────────────────┤
│  Platform:         Unity                                        │
│  Language:         C#                                           │
│  WebSocket:        Native WebSocket or Socket.IO-client-unity  │
│  HTTP Client:      UnityWebRequest                              │
│  JSON:             Newtonsoft.Json                              │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                     Infrastructure                              │
├─────────────────────────────────────────────────────────────────┤
│  Containerization: Docker                                       │
│  Orchestration:    Docker Compose                               │
│  Reverse Proxy:    Nginx (optional)                             │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📊 예외 처리 구조

```
┌─────────────────────────────────────────────────────────────────┐
│                   Exception Hierarchy                           │
└─────────────────────────────────────────────────────────────────┘

                    RuntimeException
                          │
                          ▼
                 UrikkiriException
                    (Base Custom)
                          │
         ┌────────────────┼────────────────┐
         │                │                │
         ▼                ▼                ▼
    User Domain      Card Domain      Play Domain
         │                │                │
    ├─ UserNotFound  ├─ CardNotFound  ├─ RoomNotFound
    ├─ UserExists    └─ Insufficient  ├─ RoomAlreadyFull
    └─ PasswordMis       CardsException├─ AlreadyInRoom
       Match                           ├─ ParticipantNotFound
                                       ├─ ExaminerNotFound
                                       └─ ExaminerCannotSubmitCard

         ▼                ▼                ▼
    Quiz Domain    WebSocket Domain   Security
         │                │                │
    └─ QuizNotFound  ├─ AuthRequired  ├─ InvalidJwt
                     ├─ RoomNotFound  └─ ExpiredJwt
                     ├─ NotParticipant
                     ├─ InvalidFormat
                     └─ RoomCodeRequired


┌─────────────────────────────────────────────────────────────────┐
│                  Exception Handling Flow                        │
└─────────────────────────────────────────────────────────────────┘

    Controller/Handler
           │
           │ throws Exception
           ▼
    ┌──────────────────┐
    │ ExceptionFilter  │ (for Filter chain exceptions)
    └────────┬─────────┘
             │
             ▼
    ┌────────────────────┐
    │ GlobalException    │ (for Controller exceptions)
    │ Handler            │
    └────────┬───────────┘
             │
             │ @ExceptionHandler
             │
             ▼
    ┌────────────────────┐
    │  ErrorResponse     │
    │  {                 │
    │    status: 404,    │
    │    message: "...", │
    │    timestamp: ...  │
    │  }                 │
    └────────────────────┘
             │
             ▼
          Client
```

---

## 🎯 핵심 디자인 패턴

```
┌─────────────────────────────────────────────────────────────────┐
│                     Design Patterns Used                        │
└─────────────────────────────────────────────────────────────────┘

1. Layered Architecture (계층 아키텍처)
   ┌─────────────────┐
   │  Presentation   │
   ├─────────────────┤
   │    Service      │
   ├─────────────────┤
   │    Domain       │
   ├─────────────────┤
   │  Infrastructure │
   └─────────────────┘

2. Repository Pattern (저장소 패턴)
   Service → Repository Interface → JPA Implementation

3. Facade Pattern (파사드 패턴)
   UserFacade: 여러 서비스를 조합하여 단순화된 인터페이스 제공

4. Strategy Pattern (전략 패턴)
   WebSocket Message Type별로 다른 처리 로직 수행

5. Singleton Pattern (싱글톤 패턴)
   WebSocketSessionManager, GameRoundManager (Spring @Component)

6. Observer Pattern (옵저버 패턴)
   WebSocket 방 내 사용자들에게 이벤트 브로드캐스트

7. DTO Pattern (DTO 패턴)
   Request/Response DTO로 데이터 전송 객체 분리

8. Exception Translation Pattern
   도메인 예외 → HTTP 상태 코드 변환

9. Template Method Pattern
   Spring Security Filter Chain, JPA Repository

10. Dependency Injection (의존성 주입)
    Spring의 Constructor Injection 활용
```

---

## 📈 확장 가능성

```
┌─────────────────────────────────────────────────────────────────┐
│                    Future Scalability                           │
└─────────────────────────────────────────────────────────────────┘

1. Horizontal Scaling (수평 확장)
   ┌────────────┐
   │ Load       │
   │ Balancer   │
   └─────┬──────┘
         │
    ┌────┼────┐
    │    │    │
    ▼    ▼    ▼
   App1 App2 App3
    │    │    │
    └────┼────┘
         │
         ▼
   ┌──────────┐
   │   DB     │
   └──────────┘

2. WebSocket Session 관리
   - Redis Pub/Sub로 분산 세션 동기화
   - Sticky Session 또는 Session Clustering

3. Database Optimization
   - Read Replica 추가
   - Caching Layer (Redis)
   - Connection Pooling

4. Message Queue 도입
   - 비동기 작업 처리
   - Game Result 저장
   - Ranking 계산

5. Microservices 전환 가능성
   - User Service
   - Game Service
   - Card/Quiz Service
   - Ranking Service
```

---

## 📝 요약

이 Urikkiri 서버 프로젝트는:

1. **Spring Boot 기반**의 RESTful API와 WebSocket을 결합한 실시간 게임 서버
2. **JWT 인증**을 통한 보안 강화
3. **도메인 주도 설계(DDD)** 패턴을 따르는 명확한 계층 구조
4. **WebSocket**을 활용한 실시간 멀티플레이어 게임 지원
5. **Spring Security**와 **JPA**를 활용한 엔터프라이즈급 아키텍처
6. **Unity 클라이언트**와의 원활한 통신을 위한 명확한 API 설계

---

**작성일**: 2026-01-14  
**버전**: 1.0.0  
**담당**: Backend Team

